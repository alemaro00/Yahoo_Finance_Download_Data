[project]
name = "download-data-set"
version = "0.1.0"
description = "Download and clean historical Yahoo Finance data"
requires-python = ">=3.11"
dependencies = [
    "matplotlib",
    "numpy",
    "pandas",
    "yfinance",
]

[tool.uv]
package = false
